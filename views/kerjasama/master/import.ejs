<!doctype html>
<html lang="id">
<head>
  <script>
    (() => {
      try {
        const stored = localStorage.getItem("themeMode");
        if (stored ? stored === "dark" : matchMedia("(prefers-color-scheme: dark)").matches) {
          document.documentElement.classList.add("dark");
        }
      } catch (_) {}
    })();
  </script>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %> - Facultyware</title>
  <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg" />
  <link rel="stylesheet" href="/assets/styles.css" />
</head>
<body>
  <%- include('../../partials/sidebar') %>

  <main class="main">
    <div class="p-6 max-w-2xl mx-auto space-y-6">

      <nav class="flex items-center gap-2 text-sm text-muted-foreground">
        <a href="/kerjasama/master" class="hover:text-foreground">Daftar Kerjasama</a>
        <span>/</span>
        <span class="text-foreground">Import</span>
      </nav>

      <h1 class="text-2xl font-bold">Import Data Master Kerjasama</h1>

      <% if (typeof successCount !== 'undefined' && successCount !== null) { %>
        <div class="rounded-md bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 p-4 text-green-800 dark:text-green-200 text-sm">
          ✓ Berhasil mengimpor <strong><%= successCount %></strong> data kerjasama baru.
        </div>
      <% } %>

      <% if (typeof errors !== 'undefined' && errors && errors.length > 0) { %>
        <div class="rounded-md bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 p-4">
          <p class="font-medium text-red-800 dark:text-red-200 text-sm mb-2">
            Terdapat <%= errors.length %> kesalahan:
          </p>
          <ul class="list-disc list-inside text-red-700 dark:text-red-300 text-sm space-y-1 max-h-48 overflow-y-auto">
            <% errors.forEach(e => { %><li><%= e %></li><% }) %>
          </ul>
        </div>
      <% } %>

      <div class="card p-6 space-y-4">
        <form method="POST" action="/kerjasama/master/import" enctype="multipart/form-data" class="space-y-4" id="importForm">
          <div>
            <label for="file" class="label block mb-1">
              File CSV / Excel <span class="text-red-500">*</span>
            </label>
            <input
              type="file"
              id="file"
              name="file"
              accept=".csv,.xlsx,.xls"
              class="input w-full"
              required
            />
            <p class="text-xs text-muted-foreground mt-1">Maks. 5 MB. Format: .csv, .xlsx, .xls</p>
          </div>

          <div class="flex gap-3">
            <button type="submit" class="btn">Upload & Import</button>
            <a href="/kerjasama/master" class="btn-outline">Kembali</a>
          </div>
        </form>
      </div>

      <div class="card p-6">
        <h2 class="font-semibold mb-3">Format File yang Diperlukan</h2>
        <p class="text-sm text-muted-foreground mb-3">
          Sistem membaca baris pertama berkas berdasarkan <strong>Nama Kolom Teknis</strong> berikut (wajib ditulis sama persis di Excel):
        </p>
        <div class="overflow-x-auto">
          <table class="table w-full text-sm">
            <thead>
              <tr>
                <th>Nama Kolom Teknis</th>
                <th>Nama Komponen</th>
                <th>Keterangan</th>
                <th>Wajib</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><code>partner_id</code></td>
                <td>ID Mitra</td>
                <td>ID record institusi mitra di database</td>
                <td><span class="